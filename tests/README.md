# tests

Unit tests for pure functions. No DB or network required.

Run all tests: `npm test` (or `npx vitest run`).
Watch mode: `npx vitest`.
Run one file: `npx vitest run tests/seo-analyzer.test.ts`.
